{
  "name": "chat-monorepo",
  "private": true,
  "scripts": {
    "postinstall": "cd real-time-chat-app && npm ci && npm run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
