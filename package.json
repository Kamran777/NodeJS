{
  "name": "chat-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=real-time-chat-app",
    "start": "npm run start --workspace=server"
  },
  "workspaces": [
    "real-time-chat-app",
    "server"
  ]
}